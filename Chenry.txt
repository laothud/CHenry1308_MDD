CHIRY feature list:
	Chat(Execs/Employees)

	Email(Execs/Employees)

	Work(Execs/Employees)
		Task manager(Exec/Employees access)

	User login(Facebook login)

	Exec tools(Executive Employees only)
		employee access(add/remove/update),
		work hours tracker, 
		schedule editor,
		news editor

	Admin(Full access to all feature plus the ability to remove execs and employees)